#include <stdio.h>

/**
 * main - program prints alphabet in reverse order.
 *
 * Return: Always 0 (Success)
 */ 
int main(void)
{
	int i;

	for (i = 222; i > 96; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
