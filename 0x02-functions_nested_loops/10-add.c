#include <unistd.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the first operand
 * @b: the second operand
 * Return: sum of the operands
 */
int add (int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
